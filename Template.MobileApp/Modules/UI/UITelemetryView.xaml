<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:s="http://smart/maui"
             xmlns:shell="clr-namespace:Template.MobileApp.Shell"
             xmlns:scene="clr-namespace:Template.MobileApp.Graphics.Scene"
             xmlns:module="clr-namespace:Template.MobileApp.Modules.UI"
             x:Class="Template.MobileApp.Modules.UI.UITelemetryView"
             x:DataType="{x:Type module:UITelemetryViewModel}"
             s:BindingContextResolver.Type="{x:Type module:UITelemetryViewModel}"
             shell:ShellProperty.Title="Telemetry"
             shell:ShellProperty.Function1Text="Back"
             shell:ShellProperty.Function2Text="Buffer"
             shell:ShellProperty.Function1Enabled="True"
             shell:ShellProperty.Function2Enabled="True">

    <Grid Style="{StaticResource FillGrid}"
          RowDefinitions="*">
        <scene:SceneControl Grid.Row="0" Scene="{Binding Scene}" />
    </Grid>

</ContentView>
