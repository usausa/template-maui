namespace Template.MobileApp.Modules.View;

// LazyView のデモ用コンテンツ (LoadViewAsync が呼ばれるまで生成されない)
public sealed partial class ViewStatePanelView
{
    public ViewStatePanelView()
    {
        InitializeComponent();
    }
}
